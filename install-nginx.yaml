- name: Install and start Nginx
  hosts: web
  become: true
  tasks:
  - name: Install Nginx
    ansible.builtin.service:
    - name: nginx
      state: started
  - name: Start Nginx
    ansible.builtin.service:
    - name: nginx
      enabled: true
